{
  "name": "filterable-product-table",
  "version": "1.0.0",
  "description": "Recieves user input and filters product data from the database according to the users input and displays a table of the products that match the search parameters",
  "repository": "https://github.com/Drod86/FilterableProductTable",
  "author": "Daniel Rodriguez <daniel.r.rodriguez86@gmail.com>",
  "license": "MIT",
  "source": "src/index.html",
  "browserslist": ">0.3%, defaults, supports es6-module, maintained node versions",
  "targets": {
    "default": {
      "distDir": "./docs"
    }
  },
  "scripts": {
    "start": "parcel",
    "build": "rm -rf docs/ dist/ && parcel build --public-url=''"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "parcel": "^2.9.3",
    "process": "^0.11.10"
  }
}
